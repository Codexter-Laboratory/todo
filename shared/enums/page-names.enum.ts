/**
 * This enum is used to fetch data from the Strapi CMS. These strings should match the english page names ono the strapi CMS.
 * If they do not match, you cannot fetch the correct page. IF THE NAME ON STRAPI CHANGES, THE NAME HERE MUST CHANGE AS WELL.
 */

export enum PageNames {

}

export enum CardDeckTypes{
}
